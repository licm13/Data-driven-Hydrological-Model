# 示例代码说明 / Examples Documentation

本目录包含一系列完整的、可运行的示例代码，帮助用户快速上手和深入理解水文模型的使用。

This directory contains comprehensive, runnable examples to help users quickly get started and deeply understand hydrological model usage.

---

## 📚 示例列表 / Example List

### 01. 基础模型使用 / Basic Model Usage
**文件**: `01_basic_model_usage.py`

**难度**: ⭐ 初级 / Beginner

**内容**:
- 单个模型（HBV）的完整使用流程
- 模型初始化和参数设置
- 模拟运行和结果评估
- 使用KGE和信息熵评估指标
- 结果可视化（时间序列、散点图、残差）

**运行方法**:
```bash
python examples/01_basic_model_usage.py
```

**学习目标**:
- 理解水文模型的基本使用流程
- 掌握模型参数的物理意义
- 学会使用不同的评估指标
- 了解预热期（warmup）的作用

---

### 02. 多模型性能对比 / Multiple Models Comparison
**文件**: `02_compare_multiple_models.py`

**难度**: ⭐⭐ 中级 / Intermediate

**内容**:
- 同时运行4个不同模型（GR4J, HBV, LSTM, RTREE）
- 过程驱动模型 vs 数据驱动模型对比
- 多维度性能评估（KGE, NSE, 条件熵等）
- 综合可视化（雷达图、柱状图、散点图）
- 性能排名和统计分析

**运行方法**:
```bash
python examples/02_compare_multiple_models.py
```

**学习目标**:
- 理解不同类型模型的特点和适用场景
- 掌握模型对比的科学方法
- 学会使用多个指标综合评估模型
- 了解过程驱动和数据驱动模型的差异

**关键发现**:
- 过程驱动模型：无需训练，参数有物理意义，外推能力强
- 数据驱动模型：需要训练数据，拟合能力强，但外推受限

---

### 03. 真实数据处理完整工作流 / Real Data Workflow
**文件**: `03_real_data_workflow.py`

**难度**: ⭐⭐⭐ 高级 / Advanced

**内容**:
- 真实流域数据加载和验证
- 数据质量检查（缺失值、极端值、水量平衡）
- 训练/测试集划分策略
- 模型自动校准（参数优化）
- 校准期和验证期性能评估
- 完整的数据分析报告

**运行方法**:
```bash
python examples/03_real_data_workflow.py
```

**学习目标**:
- 掌握真实数据处理的完整流程
- 理解数据质量对模型性能的影响
- 学会使用自动校准工具（SPOTPY）
- 了解模型验证的重要性

**实际应用建议**:
1. 替换为真实流域数据
2. 增加校准迭代次数（5000-10000次）
3. 尝试不同的优化算法
4. 进行参数敏感性分析

---

### 04. 学习曲线分析 / Learning Curves Analysis
**文件**: `04_learning_curves_analysis.py`

**难度**: ⭐⭐⭐ 高级 / Advanced

**内容**:
- 模型性能随训练数据量的变化
- 不同训练样本大小的实验（10-2000样本）
- 过程驱动 vs 数据驱动模型的学习效率
- 样本效率分析
- 学习曲线可视化和统计分析

**运行方法**:
```bash
python examples/04_learning_curves_analysis.py
```

**学习目标**:
- 理解学习曲线的概念和意义
- 评估模型的样本效率
- 指导数据收集策略
- 根据可用数据量选择合适的模型

**核心问题**:
- 我有多少训练数据？
- 需要多少数据才能达到满意的性能？
- 哪个模型在数据有限时表现最好？
- 增加数据量是否值得？

---

## 🚀 快速开始 / Quick Start

### 1. 确保环境已配置
```bash
# 安装依赖
pip install -r requirements.txt

# 验证安装
python -c "import src; print('✓ 安装成功')"
```

### 2. 按顺序运行示例
```bash
# 从简单到复杂
python examples/01_basic_model_usage.py
python examples/02_compare_multiple_models.py
python examples/03_real_data_workflow.py
python examples/04_learning_curves_analysis.py
```

### 3. 查看结果
```bash
# 所有结果保存在这里
ls results/examples/
```

---

## 📊 示例输出 / Example Outputs

每个示例都会生成以下类型的输出：

### 可视化图片
- PNG格式，300 DPI高分辨率
- 包含时间序列图、散点图、对比图等
- 保存在 `results/examples/` 目录

### 数据文件
- CSV格式的性能指标
- 详细的统计分析结果
- 便于后续分析和论文撰写

### 控制台输出
- 实时进度显示
- 关键指标打印
- 错误和警告信息

---

## 🎯 学习路径建议 / Learning Path

### 初学者 (Beginner)
1. ✅ 示例01：了解基本概念和流程
2. ✅ 示例02：对比不同模型（可选择2个模型快速运行）
3. 📖 阅读论文理解理论背景

### 进阶用户 (Intermediate)
1. ✅ 示例03：学习真实数据处理
2. ✅ 示例04：理解学习曲线（减少重复次数快速测试）
3. 🔧 修改参数观察性能变化
4. 📊 尝试自己的流域数据

### 高级用户 (Advanced)
1. 🔬 运行完整的实验（参见 `experiments/` 目录）
2. 📝 开发新的采样策略
3. 🎨 自定义评估指标
4. 🚀 添加新的水文模型

---

## ⚙️ 自定义示例 / Customization

### 修改数据生成参数
```python
# 在示例代码中找到这一行
data = generate_synthetic_data(
    n_days=3650,          # 修改天数
    mean_precip=3.0,      # 修改平均降水
    mean_temp=10.0,       # 修改平均温度
    mean_pet=2.0,         # 修改平均蒸散发
    seed=42               # 修改随机种子
)
```

### 修改模型参数
```python
# 在示例代码中找到参数字典
params = {
    'TT': 0.0,      # 修改阈值温度
    'CFMAX': 3.5,   # 修改度日因子
    # ... 其他参数
}
```

### 选择不同的模型
```python
# 可用模型列表
process_based = ['GR4J', 'HBV', 'SWAT+']
data_driven = ['EDDIS', 'RTREE', 'ANN', 'LSTM']

# 修改示例中的模型列表
models_to_test = ['GR4J', 'LSTM']  # 只测试这两个
```

---

## 🐛 常见问题 / Troubleshooting

### 问题1：内存不足
**症状**: 运行大规模实验时内存溢出

**解决方案**:
```python
# 减少数据量
n_days = 1000  # 从3650减少到1000

# 减少重复次数
n_replicates = 3  # 从10减少到3

# 减少训练样本大小
train_sizes = [10, 50, 100]  # 只测试小样本
```

### 问题2：运行时间过长
**症状**: 示例运行时间超过预期

**解决方案**:
```python
# 神经网络：减少训练轮次
'n_epochs': 30  # 从100减少到30

# 校准：减少迭代次数
n_iterations = 500  # 从5000减少到500

# 学习曲线：减少重复次数
n_replicates = 3  # 从30减少到3
```

### 问题3：图形不显示
**症状**: plt.show() 无效

**解决方案**:
```python
# 方案1：保存图片后手动打开
plt.savefig('output.png')
# 用图片查看器打开 output.png

# 方案2：使用Jupyter Notebook
# 在notebook中运行示例代码
```

### 问题4：数据加载失败
**症状**: FileNotFoundError

**解决方案**:
```python
# 确保数据文件存在
# 或使用合成数据（示例03会自动回退到合成数据）

# 检查数据路径
data_dir = Path('/your/actual/data/path')
if not data_dir.exists():
    print("数据目录不存在，使用合成数据")
    data = generate_synthetic_data(n_days=3650)
```

---

## 📖 参考文献 / References

这些示例基于以下论文的方法：

> Staudinger, M., Herzog, A., Loritz, R., et al. (2025).
> How well do process-based and data-driven hydrological models learn from limited discharge data?
> *Hydrology and Earth System Sciences*, 29, 5005-5029.

相关理论和方法详见论文。

---

## 💡 提示和技巧 / Tips and Tricks

### 1. 快速原型测试
```bash
# 使用小数据量快速测试代码是否能运行
# 修改示例中的 n_days=100, n_replicates=1
```

### 2. 批量运行
```bash
# 创建脚本批量运行所有示例
for example in examples/*.py; do
    echo "Running $example..."
    python "$example"
done
```

### 3. 使用配置文件
```python
# 创建 config.yaml 存储常用参数
# 在示例中加载配置
import yaml
with open('config.yaml') as f:
    config = yaml.safe_load(f)
```

### 4. 并行运行实验
```python
# 使用joblib加速
from joblib import Parallel, delayed

results = Parallel(n_jobs=4)(
    delayed(train_and_evaluate)(...)
    for _ in range(n_replicates)
)
```

---

## 🤝 贡献 / Contributing

欢迎贡献新的示例！请确保：
- ✅ 代码有详细的中文注释
- ✅ 包含完整的文档字符串
- ✅ 可以独立运行
- ✅ 生成清晰的输出和可视化
- ✅ 添加到本README中

---

## 📧 联系方式 / Contact

如有问题或建议，请提交issue到GitHub仓库。

---

## 📄 许可证 / License

MIT License - 详见项目根目录的LICENSE文件
